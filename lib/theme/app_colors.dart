import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A237E); // Deep blue from Learn More button
  static const secondary =
      Color(0xFF1E88E5); // Lighter blue for secondary (from the gradient)
  static const error = Color(0xFFB00020);

  // Light Theme Colors
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFE3F2FD); // Light Blue Surface
  static const lightOnPrimary = Color(0xFFFFFFFF);
  static const lightOnSecondary = Color(0xFFFFFFFF);
  static const lightOnBackground = Color(0xFF000000);
  static const lightOnSurface = Color(0xFF000000);

  // Dark Theme Colors
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF121212);
  static const darkOnPrimary = Color(0xFFFFFFFF);
  static const darkOnSecondary = Color(0xFF000000);
  static const darkOnBackground = Color(0xFFFFFFFF);
  static const darkOnSurface = Color(0xFFFFFFFF);

  // OTP Screen Colors
  static const otpInputBorder = Color(0xFF1A237E); // Deep blue
  static const otpInputFill = Color(0xFFE3F2FD); // Light Blue
  static const otpInputText = Color(0xFF1A237E); // Deep blue
  static const otpIconBackground = Color(0xFFE3F2FD); // Light Blue
  static const otpBorderGrey = Color(0xFFE0E0E0);
  static const otpTextGrey = Color(0xFF757575);
}
